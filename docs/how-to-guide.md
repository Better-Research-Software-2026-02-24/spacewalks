Run the code using the default data (in 'data/eva_data.json') using:

`python eva_data-analysis.py`

You can specify your own .json file as input and a .csv output with:

`python eva_data_analysis.py input.json output.csv`

The output plot will appear in a matplotlib window when you run the code. It will also be saved in the figs directory.